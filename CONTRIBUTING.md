# Contributing

1. Rule or scoring changes require **engine tests** and a **`rulesVersion` bump** (see ADR-005).
2. Complete [pre-flight checklist](./IMPLEMENTATION_PLAN.md#0-pre-flight-sign-off) before large UI work.
3. Run `pnpm test`, `pnpm build`, and `pnpm sim:random` before opening a PR.
